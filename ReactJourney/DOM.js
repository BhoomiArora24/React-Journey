//DOM(Document Object Model)
//As we know that DOM creates a whole tree like structure 

//REAL DOM
//So in real dom what happens is that if one change happens to even the smallest of the part of it the whole structure needs to reload that means the whole webpage will reload no matter that change was just in a button

//Virtual DOM
//It creates a copy of the real dom most one copy rest it depends on the code 
// for ex there is a change in the button then it will compare itself to the real dom and updates only the button 
//there is no need to reload 
//also makes the website works faster